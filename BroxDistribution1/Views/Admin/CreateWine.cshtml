@model BroxDistribution.Models.Wine

@{
ViewData["Title"] = "Add New Wine";
Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="mb-4">
    <div class="d-flex align-items-center gap-3 mb-3">
        <a asp-action="Wines" class="btn btn-outline-gold">
            <i class="bi bi-arrow-left"></i>
        </a>
        <div>
            <h1 class="brand-serif mb-0">Add New Wine</h1>
            <p class="text-muted mb-0">Fill in the details below</p>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-lg-8">
        <div class="glass-card p-4">
            <form asp-action="CreateWine" method="post" enctype="multipart/form-data">
                @Html.AntiForgeryToken()

                <div class="row g-3">
                    <div class="col-md-6">
                        <label asp-for="Name" class="form-label"></label>
                        <input asp-for="Name" class="form-control" />
                        <span asp-validation-for="Name" class="text-danger small"></span>
                    </div>

                    <div class="col-md-6">
                        <label asp-for="Brand" class="form-label"></label>
                        <input asp-for="Brand" class="form-control" />
                        <span asp-validation-for="Brand" class="text-danger small"></span>
                    </div>

                    <div class="col-md-6">
                        <label asp-for="Category" class="form-label"></label>
                        <select asp-for="Category" class="form-select">
                            <option value="">Select Category</option>
                            <option value="Red Wine">Red Wine</option>
                            <option value="White Wine">White Wine</option>
                            <option value="Rosé Wine">Rosé Wine</option>
                            <option value="Sparkling Wine">Sparkling Wine</option>
                            <option value="Dessert Wine">Dessert Wine</option>
                        </select>
                        <span asp-validation-for="Category" class="text-danger small"></span>
                    </div>

                    <div class="col-md-6">
                        <label asp-for="Country" class="form-label"></label>
                        <input asp-for="Country" class="form-control" />
                        <span asp-validation-for="Country" class="text-danger small"></span>
                    </div>

                    <div class="col-md-6">
                        <label asp-for="Region" class="form-label"></label>
                        <input asp-for="Region" class="form-control" />
                        <span asp-validation-for="Region" class="text-danger small"></span>
                    </div>

                    <div class="col-md-6">
                        <label asp-for="Grape" class="form-label"></label>
                        <input asp-for="Grape" class="form-control" />
                        <span asp-validation-for="Grape" class="text-danger small"></span>
                    </div>

                    <div class="col-md-4">
                        <label asp-for="Year" class="form-label"></label>
                        <input asp-for="Year" class="form-control" type="number" min="1900" max="2100" />
                        <span asp-validation-for="Year" class="text-danger small"></span>
                    </div>

                    <div class="col-md-4">
                        <label asp-for="AlcoholPercentage" class="form-label"></label>
                        <input asp-for="AlcoholPercentage" class="form-control" type="number" step="0.1" min="0" max="100" />
                        <span asp-validation-for="AlcoholPercentage" class="text-danger small"></span>
                    </div>

                    <div class="col-12">
                        <label for="ImageFile" class="form-label">Wine Image</label>
                        <input type="file" name="ImageFile" id="ImageFile" class="form-control" accept="image/*" />
                        <small class="text-muted">Accepted formats: JPG, PNG, WEBP (Max 5MB)</small>
                    </div>

                    <div class="col-12">
                        <label asp-for="Description" class="form-label">General Description</label>
                        <textarea asp-for="Description" class="form-control" rows="3" placeholder="General tasting notes or overview"></textarea>
                        <span asp-validation-for="Description" class="text-danger small"></span>
                    </div>

                    <div class="col-12">
                        <div class="d-flex justify-content-between align-items-center mb-3">
                            <label class="form-label mb-0">Detailed Tasting Notes</label>
                            <button type="button" class="btn btn-sm btn-outline-gold" id="addTastingNote">
                                <i class="bi bi-plus-circle me-1"></i>Add Field
                            </button>
                        </div>

                        <div id="tastingNotesContainer">
                            <!-- Dynamic fields will be added here -->
                        </div>
                        <input type="hidden" name="TastingNotesJson" id="tastingNotesJson" />
                        <small class="text-muted">Add specific attributes like Color, Aroma, Flavor, Body, Finish, etc.</small>
                    </div>

                    <div class="col-12">
                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-wine">
                                <i class="bi bi-check-circle me-2"></i>Save Wine
                            </button>
                            <a asp-action="Wines" class="btn btn-outline-gold">
                                Cancel
                            </a>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <div class="col-lg-4">
        <div class="glass-card p-4">
            <h5 class="brand-serif mb-3">Image Preview</h5>
            <div id="imagePreviewContainer" class="text-center p-5 mb-3" style="background: var(--soft-cream); border: 1px solid var(--light-gray);">
                <i class="bi bi-wine-glass display-4 text-muted"></i>
                <p class="small text-muted mt-2">No image selected</p>
            </div>
            <img id="previewImg" src="" alt="Preview" class="w-100 d-none mb-3" style="max-height: 300px; object-fit: contain; background: white; padding: 20px; border: 1px solid var(--light-gray);" />
        </div>

        <div class="glass-card p-4 mt-3">
            <h5 class="brand-serif mb-3">Quick Tips</h5>
            <ul class="small text-muted" style="line-height: 2;">
                <li>Use high-quality wine bottle images</li>
                <li>Images are saved locally in wwwroot/images/wines/</li>
                <li>Fill all fields for best display</li>
                <li>Description should be 100-200 characters</li>
            </ul>
        </div>
    </div>
</div>

@section Scripts {
<partial name="_ValidationScriptsPartial" />

<script>
    // Image preview functionality
    document.getElementById('ImageFile').addEventListener('change', function(e) {
        const file = e.target.files[0];
        const previewContainer = document.getElementById('imagePreviewContainer');
        const previewImg = document.getElementById('previewImg');

        if (file) {
            // Validate file size
            if (file.size > 5 * 1024 * 1024) {
                alert('Image size must be less than 5MB');
                this.value = '';
                return;
            }

            // Validate file type
            const allowedTypes = ['image/jpeg', 'image/jpg', 'image/png', 'image/webp', 'image/gif'];
            if (!allowedTypes.includes(file.type)) {
                alert('Invalid file type. Please use JPG, PNG, WEBP, or GIF');
                this.value = '';
                return;
            }

            const reader = new FileReader();
            reader.onload = function(e) {
                previewImg.src = e.target.result;
                previewImg.classList.remove('d-none');
                previewContainer.classList.add('d-none');
            };
            reader.readAsDataURL(file);
        } else {
            previewImg.classList.add('d-none');
            previewContainer.classList.remove('d-none');
        }
    });
</script>

<script>
    let tastingNoteIndex = 0;
    const commonLabels = ['Color', 'Aroma', 'Flavor', 'Body', 'Tannins', 'Acidity', 'Finish', 'Food Pairing'];

    function createTastingNoteField(label = '', value = '') {
        const id = tastingNoteIndex++;
        const container = document.getElementById('tastingNotesContainer');

        const fieldDiv = document.createElement('div');
        fieldDiv.className = 'tasting-note-field mb-3';
        fieldDiv.setAttribute('data-id', id);

        fieldDiv.innerHTML = `
                <div class="row g-2">
                    <div class="col-md-3">
                        <input type="text" 
                               class="form-control tasting-label" 
                               placeholder="e.g., Color" 
                               value="${label}"
                               list="labelSuggestions" />
                    </div>
                    <div class="col-md-8">
                        <input type="text" 
                               class="form-control tasting-value" 
                               placeholder="e.g., Deep ruby with purple rim" 
                               value="${value}" />
                    </div>
                    <div class="col-md-1">
                        <button type="button" class="btn btn-outline-danger btn-sm remove-tasting-note w-100" data-id="${id}">
                            <i class="bi bi-trash"></i>
                        </button>
                    </div>
                </div>
            `;

        container.appendChild(fieldDiv);

        // Add event listeners for the new field
        const inputs = fieldDiv.querySelectorAll('.tasting-label, .tasting-value');
        inputs.forEach(input => {
            input.addEventListener('input', updateTastingNotesJson);
        });

        const removeBtn = fieldDiv.querySelector('.remove-tasting-note');
        removeBtn.addEventListener('click', function() {
            fieldDiv.style.transition = 'opacity 0.3s';
            fieldDiv.style.opacity = '0';
            setTimeout(() => {
                fieldDiv.remove();
                updateTastingNotesJson();
            }, 300);
        });

        updateTastingNotesJson();
    }

    function updateTastingNotesJson() {
        const notes = [];
        const fields = document.querySelectorAll('.tasting-note-field');

        fields.forEach(field => {
            const label = field.querySelector('.tasting-label').value.trim();
            const value = field.querySelector('.tasting-value').value.trim();
            if (label && value) {
                notes.push({ label: label, value: value });
            }
        });

        document.getElementById('tastingNotesJson').value = JSON.stringify(notes);
        console.log('Updated tasting notes:', notes);
    }

    // Initialize when DOM is ready
    document.addEventListener('DOMContentLoaded', function() {
        console.log('Initializing tasting notes...');

        // Add datalist for label suggestions
        const datalist = document.createElement('datalist');
        datalist.id = 'labelSuggestions';
        commonLabels.forEach(label => {
            const option = document.createElement('option');
            option.value = label;
            datalist.appendChild(option);
        });
        document.body.appendChild(datalist);

        // Add field button
        const addButton = document.getElementById('addTastingNote');
        if (addButton) {
            addButton.addEventListener('click', function(e) {
                e.preventDefault();
                console.log('Add button clicked');
                createTastingNoteField();
            });
        } else {
            console.error('Add button not found!');
        }

        // Load existing data (for Edit page)
        const existingJson = document.getElementById('tastingNotesJson').value;
        if (existingJson) {
            try {
                const notes = JSON.parse(existingJson);
                notes.forEach(note => {
                    createTastingNoteField(note.label, note.value);
                });
            } catch (e) {
                console.error('Failed to parse tasting notes:', e);
            }
        }

        // Add initial field if none exist
        if (document.querySelectorAll('.tasting-note-field').length === 0) {
            createTastingNoteField();
        }
    });
</script>
}
